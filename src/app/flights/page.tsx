"use client";
export default function FlightsPage() {
  return (
    <div style={ minHeight: "100vh", display: "flex", alignItems: "center", justifyContent: "center", fontFamily: "sans-serif", color: "#666" }>
      <p>Flights — страница разрабатывается в другой ветке</p>
    </div>
  );
}
